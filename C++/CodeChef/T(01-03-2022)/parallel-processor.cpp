#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin >> t;
    while (t-- > 0)
    {
        int n;
        cin >> n;
        int a[n];
        int min = 0;
        bool isEqual = false;
        for (int i = 0; i < n; i++)
        {
            cin >> a[i];
        }
    }
}