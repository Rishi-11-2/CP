#include <bits/stdc++.h>
using namespace std;
#define all(v) (v).begin(), (v).end()
void solve();
signed main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.setf(ios::fixed);
    cout.precision(10);
    srand(time(NULL));
    int t;
    cin >> t;
        solve();
}
void solve()
{
    int n,c;
    cin>>n>>c;
    int a[n];
    for(int i=0;i<n;i++)
    cin>>a[i];
    function<int(int)>f=[&](int i)->int{
        if(i==n)
        {
            
        }
    };
}